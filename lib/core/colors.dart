import 'package:flutter/material.dart';
class AppColors{
  static const primaryDark = Color(0xff092B4C);   // كحلي غامق
static const primaryBlue = Color(0xff0D47A1);   // أزرق
static const cyanColor   = Color(0xff00A3A3);   // تركواز
static const greenColor  = Color(0xff00C08B);   // أخضر
static const greyLight   = Color(0xffBFC9D4);   // رمادي فاتح
static const primaryWhite   = Color.fromARGB(0, 255, 255, 255);

}

